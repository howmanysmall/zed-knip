# Gitleaks configuration for zed-knip
# https://github.com/gitleaks/gitleaks

title = "zed-knip gitleaks config"

[extend]
useDefault = true

# Add custom allowlist rules here if needed
# [[allowlist]]
# description = "Explanation"
# regexes = ['^safe-pattern$']
